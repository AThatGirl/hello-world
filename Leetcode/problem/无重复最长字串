//给定一个字符串 s ，请你找出其中不含有重复字符的 最长子串 的长度
class Solution {
    public int lengthOfLongestSubstring(String s) {
        int[] last=new int[128];
        int stare=0;//记录开始位置
        int res=0;
        int n=s.length();
        for(int i=0;i<n;i++){
            int index=s.charAt(i);
            stare=Math.max(stare,last[index]);//记下不重复字串的初始位置
            res=Math.max(res,i-stare+1);//求出不重复字母的个数
            last[index]=i+1;//将每个字母出现的位置+1，以便字母重复出现时stare可以移到该字母的下一个地方
        }
        return res;
    }
}
